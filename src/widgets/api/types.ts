export type Participant = {
  id: number;
  username: string;
  email: string;
  address?: string;
};
export interface UserData {
  id: number;
  username: string;
  email: string;
  address: string;
}
